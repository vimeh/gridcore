{
  "name": "@gridcore/ui-core",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node --external @gridcore/core && tsc --declaration --emitDeclarationOnly --outDir dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "clean": "rm -rf dist node_modules coverage",
    "generate:state-docs": "bun run scripts/generateStateDocs.ts",
    "view:state-diagram": "bun run scripts/viewStateDiagram.ts"
  },
  "dependencies": {
    "@gridcore/core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  }
}
