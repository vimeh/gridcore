import { GridCore } from "@gridcore/core";

const core = new GridCore();
console.log(`Desktop app placeholder - GridCore v${core.getVersion()}`);
console.log("Note: Desktop app will wrap the web UI using Tauri in future milestones");